# Work Log — 2026-08-11 Issue #566 PR 4 (performance evidence, ledger, closure)

Session summary: measured all five #566 performance candidates end-to-end,
recorded the evidence ledger, resolved the remaining Phase 5 item (simplett
tenferro-type re-export), routed REPOSITORY_RULES.md to shared rules by
reference, and prepared the final audit.

Code and documents read:

- `crates/tensor4all-treetn/src/treetn/evaluator.rs`,
  `crates/tensor4all-treetn/src/treetn/cached_evaluator.rs` (candidate 1/2)
- `crates/tensor4all-capi/src/treetn.rs` (candidate 2 — center persistence)
- `crates/tensor4all-tensorci/src/globalsearch.rs` (candidate 3)
- `crates/tensor4all-simplett/src/traits.rs`, `cache.rs`, `tensor.rs`
  (candidate 4, Phase 5 tenferro re-export)
- `crates/tensor4all-core/src/defaults/contract.rs` (candidate 5)
- `docs/superpowers/specs/2026-08-08-issue-566-remediation-design.md`
- `docs/superpowers/ledgers/2026-08-11-issue-566-pr4-ledger.md` (this PR's ledger)

Measurement protocol:

- Release build, default CPU/faer backend, `T4A_PROFILE_CONTRACT` unset.
- perf unavailable (`perf_event_paranoid=4`); instrumentation timing with
  `std::time::Instant`, `std::hint::black_box` on timed outputs, repeated
  runs recorded as ranges.
- Harness: `benchmarks/rust/issue566_perf_candidates*.rs` (runnable examples
  in `tensor4all-treetn` / `tensor4all-partitionedtt`).

Results (details in the ledger):

1. `TreeTN::evaluate` rebuild-per-call: 1.9–3.0% (batch), −0.4–1.5%
   (per-point) — immaterial.
2. capi cached-evaluator rebuild: with persisted center (real capi pattern)
   −0.2–0.6% (noise around 0) — immaterial; no FFI handle change.
3. global pivot search: `tt.evaluate` is 95–110% of `floating_zone` time;
   a production-equivalent batched floating_zone (per-site `TTCache`
   `evaluate_many`, result asserted equal) measured 0.31–0.55× (2–3× slower)
   — resolved by evidence, no change.
4. SimpleTT per-site allocation: random-point workloads favor per-point
   `evaluate` (TTCache 0.1×); duplicate-heavy workloads favor TTCache
   (25–107×). Zero-init removal measured 40% slower — reverted.
5. `contract_profile_enabled` env lookup: 2× env::var ≈ 148–171 ns vs
   52.8–56.9 µs per contract call = 0.26–0.32% — immaterial.

Changes: simplett re-exports `tenferro_tensor::TypedTensor` (Phase 5 item);
`REPOSITORY_RULES.md` header routes shared rules to `tensor4all-agent-rules`;
evidence ledger added under `docs/superpowers/ledgers/`.

Validation: fmt, clippy (with `-D missing_errors_doc -D missing_panics_doc`),
nextest workspace + hdf5, doctests, mdBook, check-public-error-docs.py,
check-crate-boundaries.py, audit-library-panics.py — all green. Coverage is a
CI-owned gate.
